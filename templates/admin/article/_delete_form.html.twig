{{ include('article/_delete_article_confirmation.html.twig') }}
<form action="{{ url('admin_article_delete', {id: article.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-block btn-danger">
        <twig:ux:icon name="tabler:trash"/>
        {{ 'action.delete_post'|trans }}
    </button>
</form>
